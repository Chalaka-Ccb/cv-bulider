# CV Builder - Server

Minimal Express backend for the CV Builder project.

Available scripts:

- `npm run dev` - start server with nodemon (auto-reload)
- `npm start` - start server with node

Endpoints:

- `GET /api/health` - health check
- `GET /api/cv/sample` - returns a sample CV object
